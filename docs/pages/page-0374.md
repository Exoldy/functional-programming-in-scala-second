---
title: "Page 0374"
outline: false
---

# Page 0374

[<- Page 0373](./page-0373) | [Pages index](./) | [Page 0375 ->](./page-0375)

> Part 3: Common structures in functional design / Chapter 12: Applicative and traversable functors / 12.9 Exercise answers

![Page 0374 image 1](./assets/page-0374/image-01.png)

## 345 12.9 Exercise answers

> Substitute definition of map2 in terms of flatMap and map

```scala
fa.map(f).flatMap(x => fb.map(g).map(y => (x, y)))
```

> By functor identity law, simplify consecutive map calls to a single call.

```scala
fa.map(f).flatMap(x => fb.map(b => (x, g(b))))
fa.flatMap(a => unit(f(a)))
.flatMap(x => fb.map(b => (x, g(b))))
```

> Substitute definition of map in terms of flatMap and unit

```scala
fa.flatMap(a =>
unit(f(a)).flatMap(x => fb.map(b => (x, g(b)))))
```

> By the monad associativity law, reassociate flatMaps.

> By the monad left identity law, simplify unit(f(a)).flatMap(...).

```scala
fa.flatMap(a => fb.map(b => (f(a), g(b))))
```

#### ANSWER 12.8

The `unit` operation lifts the supplied `a` into both `F` and `G` and returns the result as a pair. The `apply` operation receives a pair of functions—one in `F` and one in `G` and a pair of `F[A]` and `G[A]`. It applies the `F[A` `=>` `B]` with `F[A]` and the `G[A` `=>` `B]` with `G[A]`, returning the result as a pair:

```scala
trait Applicative[F[_]] extends Functor[F]:
self =>
def product[G[_]](
G: Applicative[G]
): Applicative[[x] =>> (F[x], G[x])] = new:
def unit[A](a: => A) = (self.unit(a), G.unit(a))
override def apply[A, B](fs: (F[A => B], G[A => B]))(p: (F[A], G[A])) =
(self.apply(fs(0))(p(0)), G.apply(fs(1))(p(1)))
```

![Page 0374 image 2](./assets/page-0374/image-02.png)

> We alias this to self so we can reference self.unit and self.apply in the anonymous instance.

![Page 0374 image 3](./assets/page-0374/image-03.png)

#### ANSWER 12.9

The `unit` operation first lifts the supplied `a` into `G` via `G.unit` and then lifts the result into `F`, resulting in a value of the `F[G[A]]` type. Unlike exercise 12.8, where we chose to implement `apply`, we’ll choose `map2` as a primitive this time. We implement `map2` by nested invocations of `map2` on the `F` and `G` applicatives:

```scala
trait Applicative[F[_]] extends Functor[F]:
self =>
def compose[G[_]](G: Applicative[G]): Applicative[[x] =>> F[G[x]]] = new:
def unit[A](a: => A) = self.unit(G.unit(a))
extension [A](fga: F[G[A]])
override def map2[B, C](fgb: F[G[B]])(f: (A, B) => C) =
self.map2(fga)(fgb)(G.map2(_)(_)(f))
```

[<- Page 0373](./page-0373) | [Pages index](./) | [Page 0375 ->](./page-0375)
