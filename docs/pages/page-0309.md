---
title: "Page 0309"
outline: false
---

# Page 0309

[<- Page 0308](./page-0308) | [Pages index](./) | [Page 0310 ->](./page-0310)

> Part 3: Common structures in functional design / Chapter 10: Monoids / 10.9 Exercise answers

```scala
as.foldRight(acc)(f)
override def foldLeft[B](acc: B)(f: (B, A) => B) =
as.foldLeft(acc)(f)
```

The `IndexedSeq` implementation does the same but also overrides `foldMap`, using `foldMapV` to more efficiently evaluate the result:

```scala
given Foldable[IndexedSeq] with
extension [A](as: IndexedSeq[A])
override def foldRight[B](acc: B)(f: (A, B) => B) =
as.foldRight(acc)(f)
override def foldLeft[B](acc: B)(f: (B, A) => B) =
as.foldLeft(acc)(f)
override def foldMap[B](f: A => B)(using mb: Monoid[B]): B =
Monoid.foldMapV(as, mb)(f)
```

The `LazyList` implementation is similar to the `List` implementation, delegating `foldLeft` and `foldRight` to the underlying data type:

```scala
given Foldable[LazyList] with
extension [A](as: LazyList[A])
override def foldRight[B](acc: B)(f: (A, B) => B) =
as.foldRight(acc)(f)
override def foldLeft[B](acc: B)(f: (B, A) => B) =
as.foldLeft(acc)(f)
```

![Page 0309 image 1](./assets/page-0309/image-01.png)

#### ANSWER 10.13

We implement all three operations, using recursive calls on branches:

```scala
given Foldable[Tree] with
import Tree.{Leaf, Branch}
extension [A](as: Tree[A])
override def foldRight[B](acc: B)(f: (A, B) => B) = as match
case Leaf(a) => f(a, acc)
case Branch(l, r) => l.foldRight(r.foldRight(acc)(f))(f)
override def foldLeft[B](acc: B)(f: (B, A) => B) = as match
case Leaf(a) => f(acc, a)
case Branch(l, r) => r.foldLeft(l.foldLeft(acc)(f))(f)
override def foldMap[B](f: A => B)(using mb: Monoid[B]): B = as match
case Leaf(a) => f(a)
case Branch(l, r) => mb.combine(l.foldMap(f), r.foldMap(f))
```

![Page 0309 image 2](./assets/page-0309/image-02.png)

#### ANSWER 10.14

```scala
given Foldable[Option] with
extension [A](as: Option[A])
override def foldRight[B](acc: B)(f: (A, B) => B) = as match
```

[<- Page 0308](./page-0308) | [Pages index](./) | [Page 0310 ->](./page-0310)
